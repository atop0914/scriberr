# Scriberr

Audio transcription tool with Whisper integration.

## Features

- Whisper.cpp integration for local transcription
- Audio format conversion (FFmpeg)
- Multiple output formats (text, JSON, SRT)
- CLI interface

## Build

```bash
go build -o scriberr ./cmd
```

## Usage

```bash
./scriberr --help
```
